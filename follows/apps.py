"""
config for follows app
"""
from django.apps import AppConfig


class FollowsConfig(AppConfig):
    """
    config for follows app
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'follows'
